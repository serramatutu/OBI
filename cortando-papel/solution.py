from sys import stdin

if __name__ == "__main__":

    # Número de retângulos
    stdin.readline()

    # Alturas
    heights = [int(x) for x in stdin.readline().split()]

    for h in heights:
        pass #TODO: Resolver o problema, hihi

    print(n)